# 项目长期记忆 · pro_orangemodel（南丰蜜桔全产业链大数据平台）

## 项目结构约定
- `html/dataanlye/` — 分析大屏（生长模型、病虫害模型两张驾驶舱）
- `html/` — 原型页面（69 个 HTML）
- `docs/` — 需求规格文档
- 交付后习惯：拿原型与大屏交叉对照，检查是否有遗漏页面或设计项

## 大屏（html/dataanlye）设计规范 —— 修改前必读
- **主题**：深色驾驶舱，底色 `#050d1a`，主色青 `#00d4ff`，token 集中在 `css/cockpit-theme.css`（`--ck-*`）
- **字体 token 单一来源**：`css/fonts.css`
  - `--ck-font-family` = 自托管 Noto Sans SC（中文）
  - `--ck-font-num` = 自托管 Barlow Condensed（数字/KPI），**不要再用 'DIN Alternate'**
  - 字体文件在 `fonts/`，离线可用；数字统一 `tabular-nums`
- **地图**：`data/nanfeng-map.js`（真实边界）+ `js/nf-map.js`（共用渲染模块），两张大屏共用，**不要各自内联一份地图配置**
  - 单一青色 5 级色阶 `#0C2A55 → #14508F → #1E7FB8 → #3CBEE6 → #74E8FB`，深浅表达数值
    （端点在 B4 中刻意拉开，原最低两级在会议室投影下几乎不可分）
  - 无数据区（长红垦殖场、南丰工业园区）用中性灰 `#3F4A5C` + 虚线边
  - 全局发光只允许出现在「告警/选中」标注点
  - 禁止：3D 投影层、扫描圈、扫描线、红黄绿热力色
- **图表配色规则**：连续型数值（评分/条数/占比）用 `NFMap.rampByRank(values)` 单色相按排名取色；
  红/橙/黄只用于状态语义（预警级别、告警）
- **大屏可读性**：字号下限 12px（`--ck-text-xs`），KPI 30px（`.kpi-number: 1.875rem`，紧凑重构后由 36px 下调），正文 14px
- **KPI 卡紧凑布局（方案 A 已落地）**：`.kpi-icon` 图标已全部移除（HTML+CSS），卡片内改 grid 三行结构
  （标题 / 数字+单位 / 同比+趋势线同行），趋势线 22px、说明文字收进容器 `title` 悬浮，
  padding 12px 14px —— 均在 `css/decision-layer.css` 统一控制，两屏共用，**不要在各屏内另写一套**
- **状态色已降饱和**（避免与青主色同高饱和互相抢注意力，只让「红」保持最高饱和）：
  橙 `#E39A3C`、黄 `#CBB04A`、深橙 `#C4832F`、升幅箭头 `#E2924A`（不要再回到 `#fbbf24/#facc15`）
- **焦点层级**：整屏 KPI 卡片**只允许 1 个强调态**（`.kpi-card.dl-focus`），其余保持基准态
- **文字对比度**：正文≥4.5:1（WCAG AA）；`--ck-text-muted` 已定为 `#94a3b8`
- **颜色替换注意**：不要用全文字符串替换改色，会破坏 `--bg-white`、`white-space` 这类标识符

## 大屏业务表达规范 —— 新增指标/面板前必读
> 背景：大屏视觉达标后，从农业农村局视角评审发现「表达不准确」。以下原则已落地，**后续改造不得回退**。

- **数据口径单一真源**：KPI、表格、图表数字一律从 JS 数据源派生，**禁止在 HTML 里另写一套数字**
  - pest：`alertDataSource.alertByType`（类型 × 红橙黄蓝 矩阵）为唯一真源，`levels` 为其列合计口径
  - 内置 `assertAlertMatrix()` 自校验，控制台报错；`verify-dashboards.mjs` 有对应硬校验
- **⚠️ 删/改数据源字段后必须跑校验脚本**（血泪教训）：
  删掉 `alertDataSource.types` 后，`renderModalStats` 里残留 5 处 `alertDataSource.types.*`，
  JS 语法检查完全合法，但该字面量求值时抛 TypeError → `renderModalDetails` 不再执行 →
  **8 个 KPI 弹窗的「核心指标 / 详情数据」全空，而 ECharts 图表照常渲染**，肉眼极易漏掉。
  现有三道防线：①引用键 ⊆ 定义键 ②每个 `openModal` 入口在 stats/details 均有配置
  ③在 vm 沙箱里真实执行渲染函数查空/查抛错。**改完数据源务必执行 `verify-dashboards.mjs`。**
- **KPI 只放动态业务指标**：禁止放「静态常量」（如病害种类数=知识库条目数）与「纯算法指标」
  （如图像识别准确率）；这两类应降级为副指标或运维视图
- **连续型数值 vs 状态语义**：同前（见「图表配色规则」）
- **样本代表性必须写清楚**：数字旁标注样本量；样本 <3 标注「仅供参考」；
  无监测点的区域**不得给评分**（用 `value:-1` 走灰色 + tooltip 说明）
- **大屏要有结论**：每屏顶部一句话播报（`.ck-brief`）；关键短板要显性成文，不能只体现在柱子高度上
- **口径诚实**：「全县平均」这类措辞必须与样本口径一致（现为「监测样本均值 n=26」）
- **责任闭环**：预警/建议需带责任人 + 完成时限，不能只有「建议」
- **阈值参照**：环境/气象数值必须给适宜区间（`growthDataSource.envRanges`），否则数字无判断价值
- **决策闭环优先于数据陈列**：大屏要能回答「现在怎样 / 最该盯谁 / 找谁办 / 在恶化还是好转」。
  已落地：待办派工清单（A1）、近 30 天 sparkline（A2）、乡镇排行+地图联动（A3）、跨屏归因（A5）
- **推算值必须可识别**：推算/测算值加 `.dl-est` 角标（虚线框「推算」角标 + `data-est` 悬浮说明算法与误差区间），
  不得与实测值同级呈现
- **同比标签要可信**：向好=青绿、需关注=红、**持平/无业务含义=中性灰 `.kpi-yoy.flat`**；
  不要出现「8 个指标 7 个向好」这种会被怀疑修饰过的分布
- **字段名不许名不副实**：曾出现 `affectedArea`（实为果园面积 23500 亩）与 KPI 在园发生面积 1860 亩
  相差一个量级，一做乡镇排行就暴露。类似「面积」类字段必须写明是哪种面积
- **柑橘术语用「在园」不用「在田」**（2026-09-11 用户确认）：「在田发生面积」是大田作物植保统计用语，
  柑橘长在果园，全屏统一改为「**在园发生面积**」（与「全县果园面积」口径呼应），勿再改回

## 病虫害业务要点（业务方关注）
- **黄龙病是柑橘毁灭性检疫病害、一号防控对象**，必须独立常驻监测，不能混在普通病害列表里；
  其媒介**柑桔木虱**的权重不可按普通虫害评「轻度」
- 防控四问：**发生多少 / 防了多少 / 效果如何 / 损失多少**
  （发生面积、防治面积、防治效果、挽回损失；另含绿色防控覆盖率、统防统治、农药减量）
- 在园发生面积按种类归因时**每块地只归因一个主要种类**，保证分项之和 = 总面积，不重复计算

## 编码硬约束（踩过坑）
- **`var` 提升陷阱**：页面顶部的自检/断言里，只能引用数据源本身（如 `alertDataSource.riskByTown`），
  **不要引用下方才赋值的 `var`（如 `var riskData = ...`）** —— 提升后会是 `undefined`，
  一 `.reduce()` 就抛错并静默中断**整个内联 script 块**，表现为「整屏数据消失」而语法检查全绿。
- 新增/修改数据源字段后，必须跑 `verify-dashboards.mjs` 的**运行时冒烟**（沙箱实跑内联脚本 + 渲染断言）。

## 常用命令
- 两张大屏回归检查：`node html/dataanlye/verify-dashboards.mjs`
  （遗留浅色值 / CSS 合法性 / JS 语法 / 地图数据完整性 / **数据口径一致性** /
  **数据源引用完整性** / **弹窗数据完整性 + 沙箱渲染实测** / **DOM 引用完整性** /
  **乡镇口径与趋势末位对齐**）
- ⚠️ **删卡片 / 改 id 后必跑**：曾出现「HTML 删了、JS 还在 `getElementById`」——
  pest 的 `applyAlertDataSource()` 第一行就抛 TypeError，导致同函数后续逻辑
  （气象灾害卡责任人渲染）**从未执行过**，页面一直显示硬编码旧内容。
  校验脚本现在会扫这类引用（有 `if (!el)` 保护的降级为 WARN）
- ⚠️ **删 ECharts 卡片必须同步删 init 与 resize**：`echarts.init(null)` 直接抛错
- 无头截图：`--headless=new --disable-gpu --hide-scrollbars --window-size=1920,1080 --virtual-time-budget=15000 --screenshot=out.png <file-url>`；
  **关键：必须加独立 `--user-data-dir`（临时目录，用后删）并用 `Start-Process -Wait` 等待写完** ——
  2026-09-11 截登录页用此法一次成功；不带独立 user-data-dir 才会复用已运行实例导致失败

## 环境限制（本机）
- Bash 工具不可用（shell shim 缺 `dirname`），统一用 PowerShell
- PowerShell stdout 不回显，结果需写入文件后读取
- PowerShell 5.1 不支持 `(if (){}else{})` 作为表达式，会 exit 1
- 无头截图失败多数因**复用用户正在运行的浏览器实例**：加独立 `--user-data-dir` + `Start-Process -Wait` 可稳定成功（见「常用命令」）；
  页面有入场动画时截图会早于动画完成 —— 需生成「禁动画副本」再截，或直接用校验脚本验证
- 不要杀 Chrome/Edge 进程 —— 用户可能正在浏览器里看这些页面

## 农事历（growth 品系分布卡）
- 卡片「当月农事历」入口 + 全年弹窗，数据真源为 growth_dashboard.html 内 `farmCalendarData`（12 个月），
  内容与后台 `html/backhand/home.html` 首页「南丰蜜桔全年农事历」逐月一致，**后台改农事历须同步改这份**
- 当月由 `new Date().getMonth()+1` 动态取，弹窗当月高亮（唯一允许的发光=选中态）
