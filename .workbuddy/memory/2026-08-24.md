# 2026-08-24 工作日志

## 南丰蜜桔模型管理系统 - 合同附件方案起草
- 解析 `docs/南丰蜜桔模型管理系统项目-功能清单.xlsx`（单工作表「功能清单」81 行），梳理出系统由 4 个子系统组成：
  1. 模型管理后台（Web 管理端：系统框架/生长模型/病虫害模型/数据管理/预警管理/微信后台/系统管理）
  2. 模型监测大屏（生长模型大屏 / 病虫害模型大屏）
  3. 蜜桔农业助手（微信小程序/移动端：首页/病虫害识别/长势评价/数据采集/农技资讯/价格行情/消息中心/个人中心）
  4. 第三方对接（单点登录/用户、设备/IoT、外部数据抓取）
- 用户需求：以功能清单为依据，起草《软件设计方案》《软件技术方案》两份 Word 文档作为合同附件，每份 ≤10 页、尽量简单。
- 区分：设计方案锁定"做什么"（功能/流程/界面/角色权限）；技术方案锁定"怎么做"（技术选型/架构/数据/接口/安全/交付）。
- 已给出大纲并获确认"现在就起草"，进入文档生成阶段。

## 两份方案修订（第二轮反馈）
- 用户要求：①补充系统架构图 ②页数保持在 10 页左右（初版仅 ~4 页，偏薄）③合同附件不加过多硬性指标约束，只用市场常见标准内容 ④字体/表格颜色一律黑色 ⑤两方案不冲突（先设计后技术）⑥按功能清单突出核心重点与流程。
- 实现：用 matplotlib(SimHei) 生成 3 张黑白架构图——总体功能架构图、核心业务闭环图（设计方案用）、技术总体架构图（技术方案用），全部嵌入 docx。
- 颜色合规：所有 run 字体色 = 000000，表格去除灰底纹改白底黑字（脚本 add_table 已移除 shd）。校验 0 个非黑 run、0 个彩色底纹。
- 两位文档结构：设计方案 8 章（概述/总体功能架构/角色权限/功能模块/关键流程/界面交互/非功能性/验收交付）+ 封面 + 目录；技术方案 9 章（技术选型架构/部署/数据/接口/模型计算/安全/性能/开发交付/运维）+ 封面 + 目录。每章强制分页，约 10–11 页。
- 生成脚本：`D:\dev\pro_orangemodel\build_contract_docs.py`（python-docx 兜底路径，因专用 html-to-docx 未部署）。产物在 `docs/`。
- 核心重点在两份文档均突出：生长模型引擎 + 病虫害模型引擎 + 预警管理 + 数据管理（核心业务引擎层）；主流程为数据采集→因子归集→模型计算→诊断分析→评价报告→预警→农事/防控建议→建议采纳（回灌闭环）。

## 技术方案新增「项目概述」章节（第三轮）
- 用户要求技术方案的「项目概述」参考 `docs/南丰蜜桔模型管理系统-整改实施方案.docx` 的相关内容。
- 整改实施方案要点：属"南丰县蜜桔全产业链智慧建设项目"；依据技术审查整改意见，在现有系统基础上完善（不新建独立平台、不大规模新增 IoT）；整改方向=生长模型/病虫害模型/销售数据收集三项；建设基础=现有后台+大屏+小程序+第三方对接；周期 2–3 个月、选代表性果园试点、农技人员参与确认。
- 在技术方案新增第 1 章「项目概述」（1.1 项目背景 / 1.2 整改与建设原则 / 1.3 建设基础 / 1.4 建设内容与范围 / 1.5 实施周期与试点），原 1–9 章顺延为 2–10 章，目录同步更新。
- 为保持两份方案不冲突，同步把设计方案 1.1「建设背景」改为「项目背景」并改写为同一项目语境（现有系统基础上完善与整改），与技术方案口径一致。
- 校验：两份文档 0 非黑 run、表格白底黑字；技术方案 10 章 + 封面 + 目录，约 11–12 页。

## 功能清单转 Word 版（第四轮）
- 用户要求把 `docs/南丰蜜桔模型管理系统项目-功能清单.xlsx` 转成 Word 版。
- 实现：新建 `D:\dev\pro_orangemodel\build_functional_list_docx.py`，解析 xlsx（5 列：系统名称/一级菜单/二级菜单/内容描述/备注），以表格形式生成 docx：4 列（备注全空已省略），子系统与一级菜单列做纵向合并分组，全黑配色，表头黑体居中。
- 产物：`D:\dev\pro_orangemodel\docs\南丰蜜桔模型管理系统-功能清单.docx`（80 行 = 1 表头 + 79 条功能，4 子系统）。校验 0 非黑 run。

## 需求说明书大更新（第五轮，基于现有 HTML）
- 用户要求按现有 HTML（backhand 后台 / dataanlye 大屏 / mobile 移动端）更新 `docs/南丰蜜桔模型管理系统-需求说明书.docx`：①第一章项目概述参考整改实施方案扩展 ②功能描述/核心字段完善丰富 ③全部截图按分辨率更新（后台+大屏 1920x1080、移动端 430x932）且无缺失 ④其他按本次修改调整。
- 截图方案：系统已装 Chrome（C:/Program Files/Google/Chrome/Application/chrome.exe），用 `--headless=new --screenshot --window-size=W,H --virtual-time-budget=20000 --force-device-scale-factor=1` 无头截图；HTML 用 Tailwind/Chart.js/ECharts CDN（网络可用）。生成 69 页 PNG 到 `docs/screenshots/`（命名 = 相对路径 /→__ + .png）。
- 关键发现：HTML 比旧文档多 7 个页面（alert/content_setting、alert/flow_setting、system/model_config、wechat/manual_collect、wechat/question_manage、mobile/mb_disease_detail、mobile/mb_disease_result）；且旧文档引用了已不存在的 growth_model_memo.html / pest_model_memo.html（回退映射 growth_model.html / pest_model.html）。
- 实现：先 `update_req_doc.py` 整体改造（重写第一章、替换 95 图、补 7 缺失页、改附录计数 46/22、全黑）；再 `fix_images.py` 按文档顺序把每张图映射到"对应文件"页、用正确分辨率 PNG 替换并清理残留旧图（首轮有 21 张弹窗截图因"对应文件"行过远未被替换，二次修复）。
- 校验结果：68 张唯一显示图，47 张 1920x1080 + 21 张 430x932，无错误分辨率、无缺失媒体、0 非黑 run；第一章整改背景/建设基础/实施周期均在；7 缺失页标题均在；附录计数已更新。`.undefined` 仅为原嵌入媒体的扩展名瑕疵，像素分辨率正确。
- 脚本：`D:\dev\pro_orangemodel\gen_screenshots.py`、`update_req_doc.py`、`fix_images.py`（均在项目根）。

## 需求说明书补充完整性（第六轮）
- 用户要求补三处：①多 Tab 页面要在功能里描述并展开每个 Tab 截图 ②第二章业务流程除文字外配流程图 ③生长大屏各统计卡片(KPI)弹窗要描述和截图。
- 多 Tab 页识别（排除移动端底部导航 `mb-tab-item`，那是页面跳转）：后台 9 个（生长模型2/基础档案3/因子采集3/农事方案3/数据校准3/病虫害数据5/预测报告3/诊断分析3/参数配置4）+ 移动端 4 个（数据采集2/数据录入5/消息中心4/档案记录6）。共 13 页。Tab 机制多为 `tab-item`/`tab-btn`/`msg-tab` + `data-tab` + `switchTab()`。
- 截图方案升级：装 `puppeteer-core`（managed node workspace，指向系统 Chrome），写 `capture_tabs.js` 在真实浏览器中点击每个 Tab / 调用 `openModal(type)` 截取弹窗；13 页 × 多标签 + 生长大屏 8 个 KPI 弹窗（主体/活跃度/植株/报告/评分/农事/采纳率/学习）= 54 张新图，全部 1920x1080 或 430x932。注意：puppeteer 脚本 require 要用绝对路径，且 `browser.close()` 在本机会挂起——已 kill 进程并手工写 `docs/screenshots/manifest.json`（labels）。
- 流程图：`gen_flowcharts.py`（matplotlib+SimHei，黑白）生成 4 张（生长模型/病虫害模型/预警管理/移动端全链路业务流程），插入第二章 2.1–2.4。
- 文档增强：`enhance_req_doc.py` 向 13 个 Tab 页"对应文件"锚点后插入"该页面含 N 个标签页…"引导句 + 各 Tab 截图与图注；向 3.3.1 大屏主界面图注后插入 8 张 KPI 弹窗截图与图注。`enhance_desc.py` 仅在每张 Tab/弹窗截图后再追加一句功能说明（41 条）。python-docx 坑：`doc.paragraphs` 每次返回新包装对象，定位须用 XML 元素（`._p.getnext()` + `Paragraph(elem, doc)`）而非 `list.index`。
- 校验：0 非黑 run；显示图 147 张（122 张标准分辨率截图 + 4 张流程图 + 21 个 .undefined 旧移动端弹窗，像素均 1920x1080）；文档 17.9 MB。
- 产物仍为 `docs/南丰蜜桔模型管理系统-需求说明书.docx`。截图源在 `docs/screenshots/`，流程图在 `docs/flowcharts/`。
