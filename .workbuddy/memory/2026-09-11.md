# 2026-09-11

## 南丰蜜桔大数据平台 · 两张分析大屏的配色/字体/地图重构

工作目录：`html/dataanlye/`

### 背景
两张驾驶舱大屏（生长模型 `growth_dashboard.html` / 病虫害模型 `pest_dashboard.html`）存在三类问题：
1. 地图用的是手绘直线多边形，乡镇划分错误且缺 紫霄镇、傅坊乡；
2. 颜色系统混乱：地图用 7 档非单调 visualMap + 红黄绿热力色，与深色青色主题冲突；同一种「长势评分」在地图上是青色、在图表里是红黄绿；
3. 字体依赖 `'DIN Alternate'`（仅 macOS 自带），Windows 上回退到 Consolas，跨机器字形不一致。

### 已完成的改动

**新增文件**
- `data/nanfeng-map.js` — 真实乡镇边界（7 镇 5 乡 + 长红垦殖场 + 南丰工业园区，共 14 个区域），
  来源 `github.com/rooma1989/china_geo_data`（WGS84），坐标简化到 5 位小数，含 `properties.cp` 质心。
- `js/nf-map.js` — 两个大屏共用的地图模块，防止样式漂移。核心导出：
  `RAMP`（单一青色 5 级 `#123A6E→#5AD7F0`）、`scorePieces()/riskPieces()`、`option()`、
  `pointIn()/centerOf()`、`alertMarkerStyle()`、`barColor()`、`rampByRank()`。
  内置 `SHORT_NAME`（长名称缩写）、`LABEL_NUDGE`（标签避让偏移）、`NODATA_FILL`（无数据区中性灰 `#3F4A5C` + 虚线边）。
- `css/fonts.css` — 自托管字体层（单一来源，cockpit-theme.css 不再重复定义 font token）。
- `fonts/` — Noto Sans SC 400/500/700（中文，约 1.1MB/字重）+ Barlow Condensed 500/600/700（数字，约 22KB）。
- `map-style-preview.html` — 地图样式对照预览页。
- `verify-dashboards.mjs` — 回归检查脚本（CSS 合法性 / JS 语法 / 遗留色值 / 依赖引用 / 地图数据完整性）。

**关键设计决策**
- 地图：单一青色系 5 级色阶，**数值由填充深浅表达，色相不承载信息**；描边固定一色；全局发光点收敛到「告警/选中」一处；去掉 3D 投影层、扫描圈、扫描线。
- 图表配色统一规则：**连续型数值（评分/条数/占比）一律单色相按排名取深浅**（`NFMap.rampByRank`）；
  红/橙/黄只保留给状态语义（预警级别、告警）。已应用到乡镇评分、品质趋势、农事建议、识别记录、病害/虫害占比饼图。
- 字体：中文自托管 Noto Sans SC（保证 Windows/macOS/Linux 渲染一致），数字用 Barlow Condensed 替代 DIN Alternate；
  所有数字启用 `tabular-nums` 防跳动；大屏最小字号抬到 12px。
- 对比度：`--ck-text-muted` 由 `#64748b`(3.7:1) 提亮到 `#94a3b8`(6.9:1)，达 WCAG AA。

### 踩过的坑（重要）
- **批量改色必须区分属性**：把 `#fff→rgba(16,40,78,0.85)` 这类替换直接跑全文，
  会把自定义属性名 `--bg-white`、`white-space` 一起改坏（`rgba(...)-space`）。
  正确做法是按声明属性名分别用「文字色表 / 表面色表」，并单次扫描替换（不要多轮级联）。
- 同一轮里**不要并行编辑同一个文件**，会互相覆盖导致部分改动丢失。
- Grep / Read 工具的结果在本会话出现过缓存，与实际磁盘内容不一致；核对最终结果用 PowerShell + `[System.IO.File]::ReadAllText` 或重新运行脚本更可靠。
- Bash 工具在本机不可用（`shim/shell-runtime-bash-env.sh` 报 `dirname: command not found`），全部改用 PowerShell。
- PowerShell 的 stdout 不回显，需要把结果写文件再 Read。
- ECharts map 的 `itemStyle.decal` 斜线填充在本版本不生效；`data` 项 `value:null` 会被丢弃（per-item itemStyle/label 失效），
  改用哨兵值 `-1` 落入 visualMap `outOfRange` 才生效。
- 本机可用的无头浏览器：Edge（`C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe`）、
  Chrome（`C:\Program Files\Google\Chrome\Application\chrome.exe`）。截图命令：
  `--headless=new --disable-gpu --hide-scrollbars --window-size=1920,1080 --virtual-time-budget=20000 --screenshot=out.png url`。
  注意用户可能正在用 Chrome 打开这些页面，**不要杀浏览器进程**。

### 业务表达评审（只分析、未改动）
从「农业农村局关心柑橘生长与病虫害防控」视角评审两张大屏，结论：视觉层已达标，但**业务表达层尚不能对外**。

**数据硬伤（最高优先级）**
- pest 大屏预警数字三处口径打架：表格「分级预警信息统计」合计 185 ≠ KPI 184；
  红色 33≠41、橙色 70≠57、黄色 35≠23、蓝色 47≠63；
  数据源 `alertDataSource.types.pest`(红23/橙45/黄0/蓝28) 与表格病害+虫害也对不上。
- KPI「气象灾害类型 3 类」与表格中 6 种气象类型（干旱/洪涝/台风/高温热害/暴雨/冰雹）矛盾。

**指标结构问题**
- growth：8 个 KPI 中 4 个是 APP 运营指标（用户主体/活跃度/报告生成数/农户学习次数），非农业指标。
- pest：8 个 KPI 中「病害种类8/虫害种类10/气象灾害类型3」是**静态常量**（知识库条目数，不随农事变化），
  「图像识别准确率 81.3%」是算法指标；真业务指标只有 4 个。

**关键业务洞察（后续改造的核心依据）**
- growth 综合评分 77.9 = 10 子项均值 ×10；其中 9 项在 7.5–8.7，**唯「病虫管理」4.7**，
  单项把综合分从约 81.3 拉到 77.9 —— 全屏最有价值的结论被埋没，图上只是一根矮柱。
- 10 子项语义混杂：植株本体(树势/叶片/根系/花果) + 产出(果实品质/膨果品质，二者重复) + 环境(温湿度/水分光照/土壤养分) + 管理行为(病虫管理)，等权平均。
- 26 株 / 11 监测点 / 12 乡镇 ≈ 每乡镇 2 株，却直接给「全县平均」，样本代表性不成立；
  地图乡镇填色缺样本量标注。
- **黄龙病在病害列表中完全缺失**（8 种为炭疽/黑点/树脂/灰霉/白绢/黑斑/褐腐/溃疡），
  而其传播媒介柑桔木虱在 Top7 中排第 5、标「轻度」—— 检疫性病害权重严重错配。
- pest 大屏缺防控核心口径：发生面积、病叶率、虫口密度、防治面积、防治效果、挽回损失、
  绿色防控覆盖率、统防统治、农药安全间隔期。
  即「发生多少/防了多少/效果如何/损失多少」四问中，后两问缺失。

**其他**
- pest 地图副标题仍是 `NANFENG · PEST RISK HEATMAP`（已改单一色阶，非热力图），文案遗留待修。
- 两张大屏均缺同比/环比、缺结论式文案、缺责任主体与行动闭环。
- growth「监测植株品系分布」卡片：进度条宽度是株数占比，表头却写「综合评分」，图文不符。

### 业务表达改造（已执行，用户要求「全部按建议执行」）

视觉层达标后，按农业农村局业务视角做了第二轮改造，重点是**让数字自洽、让结论显性、让口径诚实**。

**P0 · 数据口径（pest 大屏）**
- 原状况：`alertDataSource.types` + `unclassified` 与 HTML 表格硬编码并存两套数字（后者被 JS 覆盖不显示，
  但属维护地雷）；分类里「综合预警 5 + 未分类 18」共 23 条（12.5%）无法归类。
  **更正**：上一轮我判断「表格合计 185 ≠ KPI 184」有误——实际渲染由 JS 生成，是自洽的 184；
  真正的问题是源码双份 + 兜底分类。
- 改法：建立 `alertByType`（类型 × 红橙黄蓝 分解矩阵）作为**唯一真源**，
  删除 `types`/`unclassified`；表格改为完全由矩阵派生并按 `group` 分组（病虫害按发生程度 / 气象灾害按气象预警信号，
  依据不同故分组，不引导横向比较）；每行列均加合计，合计行直接取 `levels` 口径。
- 新增 `assertAlertMatrix()` 自校验（控制台报错），并在 `verify-dashboards.mjs` 中加了硬校验。
- `weatherDisasterKinds` 由 3 改为 6（与矩阵中 weather 组条目数一致）。
- 自洽数据：病害 62 + 虫害 60 + 干旱 7 + 洪涝 3 + 台风 2 + 高温热害 30 + 暴雨 18 + 冰雹 2 = 184；
  红 41 / 橙 57 / 黄 23 / 蓝 63 = 184。

**P1 · KPI 业务化（两屏）**
- pest：移除 3 个静态常量（病害种类 8 / 虫害种类 10 / 气象灾害类型 3 —— 知识库条目数，不会随农事变化）
  与 1 个纯算法指标（图像识别准确率 81.3%），改为 8 个动态业务指标：
  在田发生面积 1860 亩 / 活跃病害 6 种 / 活跃虫害 7 种 / 预警总数 184 / 红色预警 41 /
  处置完成率 78.3% / 已防治面积 1540 亩 / 挽回损失 126 万元。KPI 全部由 `alertDataSource` 派生。
- growth：移除 4 个 APP 运营指标（用户主体 1205 / 用户活跃度 / 报告生成 2553 / 农户学习 1420），
  改为 监测果园面积 1280 亩 / 优质果率预期 82.4% / 监测植株 26 株 / 全县预计产量 31.6 万吨。
- 全部 KPI 加同比标注，颜色按业务含义（发生面积下降=向好=青绿）而非数字增减。

**P1 · 黄龙病专项（pest 新增）**
- 黄龙病原本完全缺失，其媒介柑桔木虱在 Top7 中仅标「轻度」。新增独立常驻监测卡片
  （疑似 23 / 确诊 7 / 已砍除 7 / 涉及 3 乡镇 + 木虱诱捕 146 头周环比 +32），
  并在 Top7 木虱行加「黄龙病媒介」标签。
- 数据口径：该卡片标注「专项统计，不计入在田发生面积口径」。

**P2 · 防控四问与代表性（两屏）**
- pest 新增 `control` 对象：occurArea / controlledArea / controlEffect / savedLoss / actualLoss /
  greenControlRate / unifiedControlArea / pesticideReduction；新增 `occurByKind` 面积按种类归因分解
  （病害 990 + 虫害 870 = 1860，与 KPI 一致；每块地只归因一个主要种类，不重复计算）。
- growth 地图 `plantMapData` 增加 `samples` 字段（各乡镇 2~3 株，**合计 26 与 KPI 一致**），
  tooltip 显示样本量、样本 <3 标注「仅供参考」；
  **修正原本 scatter 的 plants 合计为 27（≠ KPI 26）**，三溪乡 4→3。
- **傅坊乡原本有评分 58 却无监测点**，改为 `value:-1` 走 outOfRange 显示灰色 + tooltip「暂无监测点，未纳入评分」。
- 「全县平均」→「监测样本均值 n=26」并加口径说明；新增短板结论：
  「病虫管理 4.7 分，低于其余 9 项均值（8.1）约 3.4 分，单项拉低综合分约 3.4 分」。
  （综合分 77.9 = 10 子项均值×10，由我核算确认。）

**P3 · 结论播报 / 责任闭环 / 阈值告警 / 两屏联动**
- 两屏顶部各加一句话结论播报（`.ck-brief`），并互相跳转（growth ↔ pest），形成闭环。
- 气象灾害预判卡片补责任人 + 完成时限（`.disaster-owner`）。
- growth 环境监测加 `envRanges` 适宜区间（土温 20~28 / 湿度 60~80 / 光照 600~1000 / pH 5.5~6.5 / 温差 8~12），
  越界时数值转橙 + 卡片描边 + 标注「偏高/偏低」；莱溪湿度改 82.1%（偏高）、洽湾 pH 改 5.2（偏酸）以体现功能。
- pest 地图副标题 `PEST RISK HEATMAP` → `PEST RISK DISTRIBUTION`（已非热力图）。
- growth「监测植株品系分布」表头由「综合评分」改为「进度条为株数占比 · 数字为综合评分」（原文图不符）。

**校验能力升级（重要）**
`verify-dashboards.mjs` 新增「数据口径一致性」模块：用括号匹配 + `vm.runInNewContext` 从 HTML 中
提取 `alertDataSource` / `plantMapData` 字面量，校验
①预警矩阵列合计 = levels ②气象类型数 ③发生面积分项和 = KPI ④活跃种类数 = 分解条目数
⑤8 个 pest KPI 的 `data-value` = 数据源 ⑥growth 样本量合计 = KPI 且无监测点不得带评分。
这样「KPI / 表格 / 数据源各写一套」的问题会被脚本直接拦下。

**环境坑（本机）**
- 无头截图**连续调用同一浏览器必然第二次失败**（Edge 与 Chrome 都一样，疑似复用已运行实例）。
  且给页面加了入场动画时，`--virtual-time-budget` 不足以让 KPI 跑完动画，需生成「禁动画副本」再截。
  最终改用校验脚本验证，比截图更可靠。
- PowerShell 5.1 不支持 `(if (...) {...} else {...})` 作为表达式，会导致 exit 1。

### 补充：顶部导航顺序约定
用户要求两张大屏顶部导航的入口顺序统一为 **生长模型在前、病虫害模型在后**
（与 `html/backhand/index.html` 侧边栏顺序一致）。`class="active"` 跟随当前页面，不随顺序移动。

### 补充：移除 pest 大屏「分级预警信息统计」卡片
用户认为该表格信息密度低、与 KPI 重复，要求整卡移除。
- 删除左侧栏第 4 张卡片（`.side-col` 内）及其 JS 渲染块（原 `alertTableBody` 生成逻辑）。
- `alertDataSource.alertByType` **保留**，仍是等级口径（`levels`）的分解依据，继续驱动
  `assertAlertMatrix()` 与 `verify-dashboards.mjs` 的口径校验，只是不再渲染到页面（已在注释中写明）。
- 顺带清理死 CSS：`.table-responsive`、`.data-table`（含历史遗留的浅色值 `color:#2c3e4e`），
  以及滚动条规则里的 `.table-responsive` 选择器；修掉一处此前批量替换产生的非法值
  `rgba(16,40,78,0.85)0db`（`.status-progress` 基类，实际被 `!important` 覆盖故无视觉影响）。
- **布局收益（巧）**：`.side-card` 固定 `height:420px` + `.side-col` `gap:20px`，
  原来 4 张卡 = 1740px 超过中央地图卡 1300px；移除后 3 张卡正好 3×420+2×20 = **1300px**，
  与 `.center-map-card` 等高，左右两栏形成对称。
- 校验：`node verify-dashboards.mjs` → 全部通过。

### 补充：结论播报去掉「挽回损失」
用户认为**挽回损失不好量化**（属测算值、难以核实），要求从 pest 大屏顶部结论播报 `.ck-brief` 中删除
「挽回损失 126 万元」，现文案为：发生面积 1,860 亩（同比 -8.4%）→ 已防治 1,540 亩、覆盖率 82.8% →
红色预警 41 条待处置 + 黄龙病 7 株已砍除 → 建议统防统治。
- 注意：`dataSource.control.savedLoss` 与第 8 个 KPI 卡片「挽回损失」**暂时保留**（用户仅要求删播报文案），
  并已向用户确认是否同步下线该 KPI。后续若删，需把 8 KPI 网格补一个替代指标
  （可选：防治效果 91.2% / 绿色防控覆盖率 68.5% / 统防统治面积 1120 亩）。

### 修复：8 个 KPI 弹窗「核心指标 / 详情数据」全空（重要事故 + 防线加固）
用户反馈弹窗数据缺失（举例：在田发生面积分析、活跃病害种类构成——图表正常但下方两块空白）。

**根因**：P0 口径改造时删除了 `alertDataSource.types`，但 `renderModalStats` 的 stats 字面量里
还残留 5 处 `alertDataSource.types.*`。JS 语法完全合法，**求值时抛 TypeError**：
- `openModal()` 内调用顺序是 `renderModalChart` → `renderModalStats` → `renderModalDetails`；
- 图表先渲染成功（所以截图里柱/环图正常、更有迷惑性）；
- `renderModalStats` 抛错后中断，紧随其后的 `renderModalDetails` **根本没执行** → 两块同时空白。
- 又因 stats 是**对象字面量整体求值**，'红色预警' 是最后一个键，一抛就全部失效 → 8 个 KPI 无一幸免。

**修法**：
- stats / details 中的 '红色预警' 改为由 `alertByType` 按 `group` 归并派生：
  病虫害类红色 16+12=28、气象灾害类红色 13，合计 41 = `levels.red`（自洽）；
  details 另加「红色预警最多 = 病害 16 条」「占总预警比 22.3%」，去掉静态的「紧急建议」。
- 顺带清掉 `levels` 注释里对已删除 `types/unclassified` 的过时描述。

**防线加固（verify-dashboards.mjs 新增 3 道）**：
1. `auditDataRefs()` —— 引用键必须 ⊆ 定义键（用 `Object.keys(字面量)` 求定义键，
   内置成员白名单过滤 `filter/map/...`）；同时校验 growth 的 `growthDataSource`。
2. `checkModalCoverage()` —— 每个 `openModal('X')` 入口必须在 `renderModalStats` 与
   `renderModalDetails` 两段里都有 `'X':` 配置。
3. `checkModalRender()` —— 用 `vm` 沙箱真跑渲染函数，逐个入口执行并断言 `innerHTML` 非空、不抛错。
   这是唯一能抓到「运行时抛错」的一层（前两层都是静态）。
4. 辅助函数 `stripComments()`：字符串感知去注释且**长度/换行不变**（行号仍准确），
   避免注释里提到某字段造成误报 —— 第一版就是被我自己写的注释 `alertDataSource.types.*` 误报的。
- 最终结果：`弹窗数据完整性：8 个入口均有核心指标与详情数据` /
  `弹窗渲染实测：8 个入口 × 2 个区块，全部有内容且无异常` / `全部通过`。

### 第三轮评审：农业农村局领导/决策者视角（只分析，未改动）
结论水位：已从「能看」进到「能管」，尚未到「能派工 / 能汇报」。
决策闭环五环中，**行动、效果回看两环是断点**（现状、风险已覆盖）。

**业务表达缺口（按优先级）**
- P0 无待办/派工闭环：184 条预警、处置率 78.3%（约 40 条未处置），但查不到是哪 40 条、
  在哪个乡镇、卡在谁手上、是否逾期；气象灾害卡有责任人+时限却没有「已完成/进行中/逾期」状态。
- P0 无时间维度：只有同比一个数，缺近 30 天/近 12 月走势（发生面积、预警数、病虫密度）。
- P1 地图不下钻、无乡镇排行（领导最常问「最该盯哪三个乡镇」）。
- P1 推算值无口径标识：全县产量 31.6 万吨等推算值与 26 株实测值视觉上无区别
  （样本口径标注只做在了 77.9 分上，应推广到所有推算值）。
- P1 两屏缺「病虫 → 长势 → 产量」影响链，互不引用。
- P1 黄金位被运维视角占用：pest「实时识别记录 / 模型预警准确率」、growth「农事建议 868 条」。
- P2 growth 8 个 KPI 有 7 个同比向好（可信度风险）；缺导出/生成简报入口。

**配色与设计**
- 焦点层级：8 张 KPI 卡 6 张同基准态，建议只留 1 个强调态（红色预警/待处置）。
- 结论播报建议拆成「形势徽标 → 关键数字 → 待办动作」，待办可点击。
- 地图信息密度低：建议叠加发生面积气泡 + 乡镇 Top5 排行 + hover 联动。
- 5 级青色阶远视距（会议室投影）下低两级难分；图例应改为「分段 + 数值区间」。
- 橙黄与青同高饱和互相抢，建议橙黄略降饱和，只让红保持最高饱和。
- 两屏不一致：growth 地图副标题用亮青 `#7dd3fc`、pest 用灰 `#94a3b8`；
  「实时监控」表述夸大（实际定时采集），建议统一为「数据更新于 xx:xx」，growth 需补更新时间。
- 滚动事件流建议悬停暂停；若会议室观看，正文 12px 偏小。`。

### 第四轮：决策层改造落地（A1–A7 / B1–B7，除「导出简报入口」按用户要求不做）

**新增共用文件（两屏共享，避免再次漂移）**
- `css/decision-layer.css` — 播报分段、sparkline、待办清单、乡镇排行、推算角标、
  图例阈值条、焦点层级、悬停暂停、字号提升。
- `js/decision-layer.js` — 全局 `DL`：`sparkline / renderSparklines / todo / rank /
  highlightRegion / bubbleSeries / bindEstTips / stampUpdate`。纯 SVG + DOM，不依赖 ECharts。

**逐项落地位置**
- A1 待办·派工：pest 左栏「实时识别记录」→「待办·派工清单」（id=`todoPanel`）；
  growth 右栏「模型农事建议」→「农事待办·派工」。逾期转红置顶，播报里的动作按钮可点击滚动直达。
- A2 趋势线：pest 发生面积/预警总数/红色预警/已防治面积；growth 综合评分/优质果率/病虫管理。
  每 2 天一点共 15 点，**末位必须等于 KPI 当前值**（已做成硬校验）。
- A3 乡镇排行：pest 右栏「模型预警准确率」→「乡镇发生面积 Top」；
  growth 右栏「农事建议落地统计」→「乡镇评分排行」。悬停联动地图高亮。
- A4 推算角标：growth 优质果率预期 / 全县预计产量；pest 挽回损失；两屏跨屏归因数字。
- A5 跨屏归因：两屏播报各加一行 `.ck-brief-cause`，含互相跳转链接。
- A6 运维指标降级：pest 实时识别记录、模型预警准确率两块下牌面，
  准确率降级为「预警」弹窗详情里的一行 `系统运行指标`；growth「农事建议数量」→「长势达标果园占比 71.2%（同比 -2.6）」。
- A7 可信度：growth 去掉「监测植株 +2 株」（无业务含义）改「覆盖 11 个乡镇 · 抽样」中性灰；
  新增负向指标打破「七绿一红」。
- B1/B2/B4/B5/B6/B7：见下。

**重要：发现并修掉一个历史隐性事故**
`pest` 的 `applyAlertDataSource()` 原本第一行就是
`document.getElementById('riskHighCount').textContent = ...`，而这些 id 在上一轮改造后
**已不在 DOM 中** → 第一行抛 TypeError → 该函数后续逻辑（**气象灾害卡的责任人与完成时限渲染**）
从未执行过，页面一直显示的是 HTML 里硬编码的旧版卡片。
修法：统一改为 `setText(id, val)` 空值保护。
→ 由此新增 `verify-dashboards.mjs` 的**第 4 道防线：DOM 引用完整性**
（扫 `getElementById('x')` 与 `id="x"` 是否对得上；已做 `if (!el)` 保护的降级为 WARN）。

**数据口径重大修正（容易踩）**
- pest 乡镇数据里原字段 `affectedArea`（2850/2430…合计 23500）**名不副实**——
  它是果园面积，不是发生面积，与 KPI「在田发生面积 1860」差一个量级。
  一旦做乡镇排行就会暴露矛盾。现拆为 `orchardArea`（果园面积，合计 23500）+ `occurArea`（发生面积，合计 1860）。
- 乡镇 `warningCount` 合计改为 184（= 预警总数）；`incidence` 改为由 occurArea/orchardArea 现算。
- `riskTowns` 修正为 `{high:2, mid:6, low:4}`（阈值 高≥80 / 中 55–79 / 低<55，合计 12 = 乡镇数）。
- 乡镇数据整体搬进 `alertDataSource.riskByTown`（单一真源），页面用 `var riskData = alertDataSource.riskByTown;`
  —— 这样弹窗渲染函数在校验沙箱里也能取到，沙箱实测才跑得通。
- growth `kpis` 键名重写为与页面 8 个 KPI 一一对应（monitorArea/qualityRate/plants/yield/
  score/qualifiedRate/adoptRate/pestScore），删掉 users/activity/reports/suggestions/learnTimes 死数据。

**配色调整**
- `js/nf-map.js` RAMP 端点拉开：`#123A6E…#5AD7F0` → `#0C2A55…#74E8FB`（B4 远视距）。
- 全局降饱和（B5）：`#fbbf24→#E39A3C`、`#facc15→#CBB04A`、`#f59e0b→#DE9A4E`、
  `#d97706→#C4832F`、`#FF7E29→#E2924A`（两屏共 67 处，正则 `(?![0-9a-fA-F])` 防止误伤更长 hex）。
- 字号（B7）：KPI 数字 1.7rem → 2.25rem(36px)，卡片标题 15px。
- 删除的图表：`recognitionChart`、`accuracyTrendChart`、`farmingSuggestionChart`
  （含 init + resize 全部清理，否则 `echarts.init(null)` 抛错）。

**当前校验输出**：`node verify-dashboards.mjs` → 全部通过
（4 条 WARN 为 growth 里 `currentDate / mapPopup / popupTitle / popupContent` 死引用，已有空值保护）。

### 修复：pest 大屏「整屏数据消失」（var 提升导致的静默中断，最严重的一次）

用户反馈 pest_dashboard.html 所有数据都没了（KPI 显示 0、地图/待办/排行/趋势线全空）。

**根因**：`assertAlertMatrix()` IIFE 位于 HTML 第 ~1882 行，里面新增的乡镇口径校验用了
`riskData.reduce(...)`；而 `var riskData = alertDataSource.riskByTown;` 直到第 2091 行才赋值。
`var` 提升 → 执行到断言时 `riskData === undefined` → `.reduce` 抛 TypeError
→ **整个内联 script 块从这里往后的代码全部不执行**（地图 setOption、KPI 同步、animateNumbers、
待办、排行、趋势线、气象灾害卡）。而 JS 语法完全合法，静态检查一列全绿。

**修法**：断言内改用 `var towns = alertDataSource.riskByTown;`（直接取真源，不依赖后面才声明的变量），
并加注释说明「数据源定义之后才声明的变量，不要在断言里引用」。

**教训（通用）**：在页面顶部执行的自检/断言，只能引用数据源本身，**不要引用下方才赋值的 `var`**。
`var` 提升会让它静默变成 undefined，且中断的是整个脚本块。

**新增第 5 道防线：运行时冒烟（verify-dashboards.mjs 的 runtimeSmoke）**
用 `vm` + 极简 DOM mock 把每段内联脚本（及本地外链 js）真跑一遍，并做**正向渲染断言**：
- 每段内联脚本执行完成、无异常（能抓 var 提升 / undefined / 顺序错误这类纯运行时故障）；
- `data-spark` 容器执行后 `innerHTML` 必须含 `<svg`（趋势线真的画出来了）；
- `#todoList` / `#townRank` / `#disasterList` 执行后必须非空。
  mock 策略：`getElementById` / `querySelector` 一律返回元素（缺元素由「DOM 引用完整性」检查负责），
  `querySelectorAll` 仅对 `[data-spark="x"]` 按 HTML 中出现次数返回对应数量的 mock 节点。
最终输出：`pest 已渲染 7 处：occurArea、alertTotal、redAlert、controlledArea、#todoList、#townRank、#disasterList`；
`growth 已渲染 5 处：score、qualityRate、pestScore、#todoList、#townRank`。

**顺带修掉**：pest「在田发生面积」KPI 缺 `data-spark="occurArea"` 承接容器，
`trends.occurArea` 序列定义了却从未渲染（已补）。

**本机环境补充**：Edge 无头 `--dump-dom` / `--screenshot` 在本机会输出空文件（疑似被用户正在运行的
浏览器实例占用），无法作为验证手段；**以 `verify-dashboards.mjs` 的沙箱实跑为准**。


### 补充：KPI 卡紧凑重构（方案 A，2026-09-11 晚）
用户确认统计卡偏大，选定方案 A 落地：
- 两屏共 16 个 .kpi-icon 图标节点及全部相关 CSS 已移除（趋势线接管其视觉职责）
- css/decision-layer.css：.kpi-info 改 grid 三行结构（标题 / 数字+单位 / 同比+趋势线同行），
  趋势线 30→22px、说明文字改容器 	itle 悬浮（JS sparkline() 同步删 cap 逻辑）、
  数字 2.25rem→1.875rem、.kpi-card padding 12px 14px —— 卡高约 150→100px
- 省出的高度补给中央地图：两屏地图容器 650→700px，center-map-card 同步 +50（pest 1350 / growth 1210）
- 
ode verify-dashboards.mjs 全部通过（沙箱冒烟 5/7 处渲染正常）
- 注：cockpit-theme.css 的 .ck-kpi-icon 与 dashboard-design-system.css 的 .ds-kpi-icon 是带前缀通用库样式，未动
- 修复：pest 气象灾害卡溢出 —— 6 张灾害子卡(各4行)超出 420px 卡高被静默裁剪，#disasterList 改为 flex+overflow-y 滚动区(含青色滚动条)，子卡 padding 14→10px、间距 10→8px
- 修复：growth 环境监测卡溢出 —— 同 pest 气象卡模式：.compact-card 固定 280px，JS 注入「适宜区间」行后 3 行子卡放不下被裁剪。.env-grid 改 flex+overflow-y 滚动区(6px 青色滚动条)，子卡 padding 10→7/8、图标 36→32、文字行高收紧（注：两列总高 1160 与中央地图卡对齐，不能加高卡片）
- 22:28 术语修正：经用户确认，两屏「在田发生面积」统一改为「在园发生面积」（柑橘为果园作物，「在田」是大田作物用语）；涉及 pest_dashboard.html 约 20 处、growth_dashboard.html 1 处（跨屏归因文案）、verify-dashboards.mjs 3 处；回归校验全部通过。
- 跨屏归因瘦身（方案①）：播报区只保留可实测内容（pest 留「占全县果园 7.9%」、growth 留「对应病虫害屏 1,860 亩」），删除全部模型外推数字（拉低 1.1/0.6 分、产量 0.9%、优质果率 0.8pct）；推算逻辑移入 KPI 弹窗详情（pest「发生面积」、growth「病虫管理」各加一行并标注模型推算与误差）。原则：**播报区只放测得准的数，推算值留给弹窗**
- 22:40 pest 大屏左列调整：黄龙病专项卡由固定 420px 改自适应高度（消除底部约 170px 空白），新增「病虫害识别图谱」卡（flex:1 吸收空高，4 列 8 格：黄龙病/木虱/红蜘蛛/锈壁虱/潜叶蛾/炭疽病/黑点病/溃疡病，各格链接后台 disease_db / insect_db，标注知识库速查非监测数据）；列总高仍 1300px 对齐地图；回归校验全部通过。无头截图再次失败（0 字节，已知本机问题），布局以校验脚本为准。
- 22:50 图谱卡按用户反馈返工：4 列 8 格撑宽 → 改 2 行 × 3 列 6 格（保留黄龙病/木虱/红蜘蛛/锈壁虱/潜叶蛾/炭疽病，去掉黑点病/溃疡病）；点击不再跳转后台页面，改为页内弹窗 openAtlasModal（独立 atlasOverlay，不占用 openModal KPI 配置），弹窗内容取自后台 disease_db / insect_db 字段口径（识别特征/为害症状/发生规律/防治要点/注意事项），并注明内容来源与维护入口；图块补 min-width:0 防止 nowrap 撑宽；回归校验全部通过。

### 品系口径修正（2026-09-11 晚，用户确认）
本县近半年/一年实际主要监测 **3 个品种**：小果系（杨小2-6、金钱蜜桔）、早熟系（普通早熟蜜桔）、蜜广1号（晚熟蜜广）。
growth 大屏已按此修正，后续不要再出现 大果系/桂花蒂系/桂月红/日南1号 等旧品种：
- 「监测植株品系分布」卡：6 行→3 行（9/9/8 株，79.2 / 75.1 / 79.8 分，加权均值≈78.0 与原口径一致）
- evalObjBuilder.typeConfig、entityData、scrollingEntities：6 品系→3 品系；11 个监测点重分桶
  （service [2,2,3,2] / demonstration [2,3,2,2] / iot [2,2,4]），评分/人员/日期记录不变
- growthDataSource.samplePoints 标签从主体类型（服务/企业/…）改为品系名
- 优质果率弹窗（stats/chart/details）只保留三品系：84.2 / 82.8 / 78.4，全县预期 82.4% 不变

## 农事历上线（growth 品系分布卡，2026-09-11 晚）
- 品系分布卡留白处新增「当月农事历」入口：按 `new Date()` 动态取当月，显示 月份+物候期+节气+农事要点，整块可点（含 Enter/Space 键盘可达）
- 点击打开「南丰蜜桔全年农事历」弹窗（#farmCalendarOverlay，920px 宽，3列12月网格，当月高亮+「当月」角标）
- 数据单一真源：`farmCalendarData`（12个月）与后台管理系统 html/backhand/home.html 全年农事历逐月一致；卡片入口与弹窗均由此派生
- 新增 CSS：`.farm-cal-*` 系列深色样式（青色系、无扫描线/3D，hover 只变背景边框不加发光）；字号下限 0.75rem
- verify-dashboards.mjs 全部通过
- 23:00 图谱卡再调：卡片名「病虫害识别图谱」→「病虫害图谱」；弹窗标题下新增一行 3 张症状/形态图（等宽 108px 高 cover，素材 URL 取后台 disease_db/insect_db 的 images 字段，图片加载失败显示占位图标，图行下标注素材来源）；其余内容不变；回归校验全部通过。

## 后台登录页改版（html/backhand/login.html，2026-09-11 深夜）
- 参考用户截图（满屏农田实景 + 居中半透明圆形磨砂登录卡 + 绿色登录按钮）重写样式
- 布局：删原「左品牌区 + 右白卡」双栏 → 满屏背景图 + 居中 480px 圆形磨砂卡（rgba 白 0.72 + backdrop-filter blur 14px）
- 配色：按钮/焦点改绿色系（#67995C→#4C7F42 渐变），logo 保留橙色蜜桔 SVG；toast.success 同步改绿
- 文案：标题「南丰蜜桔模型管理」+ EN 副标 + 标语「蜜桔智慧管理 · 一站监管」（仿参考图结构）
- 交互 JS（togglePassword/showToast/handleLogin/跳转 index.html）原样保留
- 截图验证经验（更新）：Edge 无头首截失败；**Chrome --headless=new + 独立 --user-data-dir + Start-Process -Wait 成功**——之前「必失败」结论仅适用于不带独立 user-data-dir 的调用，本机截登录页一次成功，临时 profile 用后即删

## 登录页二改：对齐 evo.dvpc315.cn 参考站（同日深夜）
- 用户指定参考 https://evo.dvpc315.cn/Login/Index/filing（品牌数字化备案管理系统）——WebFetch 只能取到文字（JS 渲染），**无头 Chrome 直接截远程 URL 成功拿到完整视觉**
- 参考站要点：绿色田野背景 + 居中近实心白圆卡 + 圆外装饰（贴身虚线刻度环 -14px / 外圈缺口细弧线 -30px 旋转 -30°）+ 绿 logo + 深色标题 + 绿色 EN 副标 + 标语「打造品质标杆·塑造农业品牌」+ 亮绿按钮（约 #3cb04a）
- login.html 相应改动：卡片 rgba 白 0.93（去 backdrop blur）+ ::before 刻度环 + ::after 缺口弧线；logo 换绿色渐变圆+叶形 SVG；EN 副标绿色 #3cb04a 并 nowrap 防换行；按钮/焦点/复选框/链接/toast 全部统一到 #3cb04a→#2FA847 绿色系
- agent-browser CLI 在本机不可用（其捆绑 Chrome 报 DevToolsActivePort 启动失败），直接用系统 Chrome 无头更稳
- 登录页素材：背景沿用 nanfeng_mandarin_orchard.jpg（绿色调与参考站田野氛围一致）

## 登录页三改：完全对齐图 2（同日深夜，最终版）
- 用户对比后选定 evo 参考站风格为最终目标，要求删掉 记住密码/忘记密码/©版权 等元素，字体/字号/圆的大小/动态/按钮/背景全部对齐
- 已删：记住密码、忘记密码、login-footer 版权；占位符改「请输入账号/请输入密码」（同参考站）
- 版式：圆 480→640px；品牌区改「logo(60px) + 南丰蜜桔(27px 粗体 #333) + 72×3 绿色下划线 + NANFENG MANDARIN(绿 12px)」横排；标语带虚线下划线；按钮纯色 #3cb95d、radius 4、字距 12px
- 动态：三层装饰环缓慢旋转（外大环 80s / 缺口弧 50s 反向 / 虚线刻度 120s），prefers-reduced-motion 时停用
- **背景图已换**：ImageGen 生成「青绿麦田+远山+蓝天」（html/dataanlye/images/login_bg_field.jpg，1536×901）；原图带 AI 水印，用 Pillow 裁掉底部 12% 解决（Add-Type System.Drawing 被沙箱拦截，Python venv 有 Pillow 12.3.0 可用）
- 大屏截图验证：视觉与图 2 高度一致

## 登录页四改：文案与背景定稿（同日深夜）
- 标题「南丰蜜桔」→「南丰蜜桔模型管理系统」（26px，去 logo 图标），EN 同步为 NANFENG MANDARIN MODEL MANAGEMENT SYSTEM（10.5px/字距 2px 防折行）
- 标语改「精准辅助生长、科学指导防控」；背景换回 nanfeng_mandarin_orchard.jpg（用户指定南丰蜜桔果园图）
- login_bg_field.jpg（AI 生成麦田图）保留在 dataanlye/images 备用，当前未被引用

## 登录页五改：背景图定稿（同日深夜）
- 用户提供南丰县政府网站 banner 截图（蜜桔+远山+高铁），要求去文字后做登录背景
- 用 ImageGen 图生图（input_fidelity: high）去除全部文字/徽标，Pillow 裁剪 1024×1024→1024×576（y 330~906，同时裁掉右下 AI 水印），存为 html/dataanlye/images/login_bg_orchard.jpg
- login.html 背景已切换到该图；nanfeng_mandarin_orchard.jpg 不再被登录页引用（大屏等其他页面可能仍用，勿删）

## 登录页六改：字号/控件尺寸对齐图 1 高清截图（同日深夜，当前定稿）
- 拿到 evo 参考站高清截图后按比例精调：标题 26→32px/字重 800/字距 4px；EN 10.5→13px/字重 500；标语 16→20px、字距 5px
- **删除了自创的标题绿色下划线条与标语虚线下划线**（图 1 中两者都没有）
- 输入框 380×42→440×50、字 15px、图标 16px；按钮高 44→52px、字 20px、字距 16px；表单间距收紧（组间 14px）
- 卡片内容宽 380→440px；截图验证比例与图 1 一致

## 登录页七改：按参考站源码精确定稿（同日深夜，最终版）
- 抓到 evo 参考站真实 CSS/源码（Invoke-WebRequest 抓 HTML + 内联样式；无头 dump-dom 本机输出空文件不可用）：
  - 圆 = 7.5rem，根字号 calc(100vw/19.2) → 1920 宽时 750px，随视口缩放
  - 圆框是 kuang.svg（750×750）：淡白晕环 r375(op .117) + 白色双 1/4 弧线 r347(粗8) + 主白圆 r331(op .9) + 绿虚线环 r315(#cfedd4 粗9 dasharray 2,16)
  - **转动频次：SVG 内 SMIL animateTransform 15s/圈**（并非我们猜的 50~120s）
- login.html 已按源码重写：orb-wrap clamp(480px,39.06vw,820px)、环改为内联 kuang.svg 复刻 + CSS spin 15s（reduced-motion 可停）、主圆 inset 5.87% / rgba 白 .9、内容宽 58.7%
- 内容尺寸按 rem 换算（1rem=100px@1920）：标题 40px/字距2、EN 15px #47a14b、标语 20px #666 字距5、输入框 440×56 边框#E3E3E3 圆角6 字16px、按钮 440×60 #4caf50 圆角6 字21px 字距4 hover #3f9542；主色从 #3cb95d 系统一切换到 #4caf50 系
- 截图验证：圆径、弧线方向、虚线密度、控件比例均与参考站一致
- 23:05 pest 大屏待办卡溢出修复：按用户要求删除 mock 数据「太和镇溃疡病发病园复查」一条（alertDataSource.todos 5 条），卡片头部计数由 decision-layer.js 按列表长度自动派生，自动变为「重点待办 5 项，其中逾期 2 项」；回归校验全部通过。
- 追加：用户要求当月农事历为独立卡片（不内嵌品系分布卡）→ 已拆为 side-card（品系分布卡下方），入口改为完整展示当月3条农事+节气，删除 .farm-cal-divider 内嵌样式，校验通过
- 23:10 待办卡溢出根治：todoPanel 改 flex:1 撑满左列剩余高度（图谱卡改 flex:0 0 auto 内容自适应），#todoList 改 flex 纵向容器、内层 dl-todo-list 滚动兜底（head/备注钉住）；三列同属 1300px 栅格行，左列底边与中列事件流、右列气象灾害卡严格对齐；回归校验全部通过。
- 23:14 纠正上一步误改：图谱卡恢复 flex:1 / min-height:140px 原样式；溢出修复只作用于待办卡——卡片保持 420px 不变，#todoList 改 flex 纵向容器，内层 dl-todo-list 滚动兜底（计数头与底部备注钉住）；回归校验全部通过。
- 追加：品系分布卡(230px)+当月农事历卡(170px)压缩高度，左列合计 420+280+230+170+3×20=1160 与中列 center-map-card、右列底部对齐；新增 .variety-slim-card/.farmcal-slim-card（覆盖 side-card 的 min-height:260/height:420），窄屏媒体查询恢复自适应
- 23:20 左列对齐根治（经截图实测迭代）：atlas 卡锁 flex:0 0 140px（视觉与首版一致）；Top7 卡加 top7-card 改内容自适应（消除 420px 固定空腔）；hlb 卡内部间距压缩（body/alert/grid/item/foot）；待办卡 flex:1 1 auto 填满剩余高度（底边与中列事件流、右列灾害卡对齐），todo 项/头本地压缩间距，5 条全部可见无滚动；截图确认三列底边齐平；回归校验全部通过。注：本机无头截图带独立 user-data-dir + Start-Process -Wait 可稳定成功。
- 23:25 按用户要求 Top7 → Top5：卡片标题改「病虫害种类 Top5」，删除黑点病/溃疡病两条 mock（剩红蜘蛛/锈壁虱/潜叶蛾/炭疽病/柑桔木虱·媒介），样式钩子类名 top7-card → top5-card；图谱卡 140px 高度未动；截图确认三列底边仍齐平、待办 5 条全可见；回归校验全部通过。
- 23:30 按用户反馈三处调整：①Top5 卡标题改「病虫害本月发生 Top5」（header-extra 改「按严重程度排序」）；②图谱 6 格去掉「病害/虫害·严重度」副标题，仅留图标+名称（连同 atlas-text/atlas-sub CSS 一起清理）；③待办 mock 加回两条（太和镇木虱成虫加密监测 09-17、傅坊乡红蜘蛛虫口密度复查 09-18，共 7 条，计数头由 decision-layer 自动派生为 7 项/逾期 2）；截图自查三列底边仍齐平、7 条全部可见；回归校验全部通过。
- 23:40 图谱卡溢出根治：atlas-card 由 140px 提到 172px（header+两行格子+内边距实测需 170px）；连带 todo 卡少了 32px 空间导致第 7 条被卷走，用 DOM 实测（临时副本注入测量浮层，dump-dom 不可用）确认 todoPanel sh=546>h=518 后，把 dl-todo-item 压到 padding 3px/margin 2px、head/foot-note 同步微压，复测 sh=h=518 无溢出；回归校验全部通过。测量技巧：headless dump-dom 本机输出 0 字节，改用临时副本注入 fixed 定位浮层写测量值再截图读取，可靠。
- 23:45 待办卡回填：发现上轮新增的 2 条 todo 数据被外部覆盖丢失（文件仅剩 5 条，其余改动完好，疑似编辑器旧缓冲保存所致），重新补回太和镇木虱监测/傅坊乡红蜘蛛复查（共 7 条，计数头自动派生 7 项/逾期 2）；dl-todo-list 加 flex 纵向 + dl-todo-item flex:1 1 auto 弹性分布，条数不足时自动摊满卡片不留空腔、条数超限时内层滚动兜底；DOM 实测 h=sh=518 恰好填满；回归校验全部通过。
