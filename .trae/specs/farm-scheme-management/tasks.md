# 农事建议配置管理 - 实施计划

## [x] 任务 1: 创建页面基础结构和导航
- **优先级**: 高
- **依赖**: 无
- **描述**:
  - 创建 `farm_scheme_config.html` 页面基础结构
  - 在 `index.html` 生长模型菜单下新增「农事建议配置」菜单项
  - 添加页面标题、面包屑导航
  - 引入 Tailwind CSS 和基础样式
- **验收标准**: AC-1
- **测试要求**:
  - `programmatic` TR-1.1: 页面可正常加载，无 404 错误
  - `human-judgement` TR-1.2: 菜单项显示「农事建议配置」，点击可跳转
- **备注**: 页面路径: `growth/farm_scheme_config.html`

---

## [x] 任务 2: 实现农事建议方案列表展示
- **优先级**: 高
- **依赖**: 任务 1
- **描述**:
  - 迁移 `farmSchemeDB` 数据到新页面（默认数据）
  - 实现方案列表表格（ID、因子类型、优先级、适用阶段、措施摘要、操作）
  - 实现类型统计卡片（按因子类型统计方案数量）
  - 实现优先级统计卡片
- **验收标准**: AC-1
- **测试要求**:
  - `programmatic` TR-2.1: 列表正确显示所有方案，数据与默认数据一致
  - `human-judgement` TR-2.2: 表格布局清晰，方案信息完整

---

## [x] 任务 3: 实现筛选和搜索功能
- **优先级**: 高
- **依赖**: 任务 2
- **描述**:
  - 添加关键词搜索框（搜索方案ID、因子类型、措施内容）
  - 添加因子类型筛选下拉框
  - 添加优先级筛选下拉框
  - 添加生育阶段筛选下拉框
  - 实现多条件组合筛选
  - 显示筛选结果统计
- **验收标准**: AC-5
- **测试要求**:
  - `programmatic` TR-3.1: 设置筛选条件后，列表正确过滤
  - `programmatic` TR-3.2: 清除筛选后显示全部数据

---

## [x] 任务 4: 实现新增/编辑方案表单
- **优先级**: 高
- **依赖**: 任务 2
- **描述**:
  - 创建方案编辑弹窗（新增/编辑复用）
  - 实现表单字段：
    - 因子类型（下拉选择）
    - 偏离方向（下拉：偏高/偏低/失调）
    - 适用生育阶段（多选复选框组）
    - 优先级（下拉：urgent/important/normal/low）
    - 详细措施（动态列表，支持增删多条）
    - 复查周期（文本输入）
    - 复查监测指标（文本输入）
    - 预期调节效果（文本输入）
  - 表单验证（必填项、阶段至少选一个）
  - 保存后更新列表
- **验收标准**: AC-2, AC-3
- **测试要求**:
  - `programmatic` TR-4.1: 新增方案后，列表显示新方案
  - `programmatic` TR-4.2: 编辑方案后，修改生效
  - `programmatic` TR-4.3: 缺少必填字段时提示错误

---

## [x] 任务 5: 实现删除功能
- **优先级**: 高
- **依赖**: 任务 2
- **描述**:
  - 删除确认弹窗
  - 单个删除功能
  - 删除后更新统计和列表
- **验收标准**: AC-4
- **测试要求**:
  - `programmatic` TR-5.1: 确认删除后方案从列表移除
  - `programmatic` TR-5.2: 取消删除后列表不变

---

## [x] 任务 6: 实现方案预览/测试功能
- **优先级**: 中
- **依赖**: 任务 2, 任务 4
- **描述**:
  - 添加「方案测试」Tab
  - 实现模拟扣分项输入表单（因子、实测值、生育阶段）
  - 集成匹配引擎（复用 `matchFarmScheme` 逻辑）
  - 显示匹配结果列表
  - 高亮显示匹配的方案
- **验收标准**: AC-6
- **测试要求**:
  - `programmatic` TR-6.1: 输入扣分项后显示匹配方案
  - `human-judgement` TR-6.2: 测试结果清晰展示

---

## [x] 任务 7: 实现因子映射管理
- **优先级**: 中
- **依赖**: 任务 2
- **描述**:
  - 添加「因子映射」Tab
  - 迁移 `factorMapping` 数据
  - 实现映射列表（因子名 → 建议类型）
  - 新增/编辑/删除映射关系
  - 显示映射使用情况（被几个方案引用）
- **验收标准**: AC-8
- **测试要求**:
  - `programmatic` TR-7.1: 映射增删改正确生效
  - `programmatic` TR-7.2: 映射影响匹配结果

---

## [x] 任务 8: 实现数据持久化
- **优先级**: 中
- **依赖**: 任务 4, 任务 5, 任务 7
- **描述**:
  - 使用 localStorage 保存 `farmSchemeDB` 和 `factorMapping`
  - 页面加载时从 localStorage 读取数据
  - 提供「恢复默认数据」按钮
  - 提供「导出配置」功能（导出为 JSON）
- **验收标准**: AC-7
- **测试要求**:
  - `programmatic` TR-8.1: 刷新页面后数据保持
  - `programmatic` TR-8.2: 恢复默认后数据重置

---

## [x] 任务 9: 集成诊断分析页面数据同步
- **优先级**: 中
- **依赖**: 任务 8
- **描述**:
  - 更新 `diagnosis.html` 从 localStorage 读取 `farmSchemeDB`
  - 确保两个页面数据一致
  - 实现实时数据同步（storage 事件监听）
- **验收标准**: AC-7
- **测试要求**:
  - `programmatic` TR-9.1: 在配置页修改后，诊断页使用最新数据
  - `human-judgement` TR-9.2: 数据同步无延迟

---

## [x] 任务 10: 样式优化和整体验证
- **优先级**: 低
- **依赖**: 全部任务
- **描述**:
  - 检查 UI 一致性（与系统其他页面风格统一）
  - 添加 Toast 提示组件
  - 添加加载状态动画
  - 响应式布局检查
  - 性能优化
- **验收标准**: AC-1
- **测试要求**:
  - `human-judgement` TR-10.1: 整体视觉风格与系统一致
  - `human-judgement` TR-10.2: 交互流畅，无卡顿
